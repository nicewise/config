# config
+ set wallpaper(s)
```bash
  ln -sf /path/to/real/picture $HOME/config/conf.d/background
  ln -sf /path/to/real/picture $HOME/config/conf.d/wallpaper1
  ln -sf /path/to/real/picture $HOME/config/conf.d/wallpaper2
```
+ bash deploy.sh
+ push update
